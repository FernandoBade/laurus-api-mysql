/** @summary ISO 8601 date-time string in UTC. */
export type ISODateString = string;

/** @summary Monetary value serialized as a string to avoid float rounding. */
export type MonetaryString = string;