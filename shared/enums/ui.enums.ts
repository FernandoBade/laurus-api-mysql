/** @summary Visual intent variants for reusable button components. */
export enum ButtonVariant {
    PRIMARY = "primary",
    SECONDARY = "secondary",
    ACCENT = "accent",
    OUTLINE = "outline",
    GHOST = "ghost",
    LINK = "link",
}

/** @summary Size tokens for button components. */
export enum ButtonSize {
    SM = "sm",
    MD = "md",
    LG = "lg",
}

/** @summary Semantic variants for alert and feedback components. */
export enum AlertVariant {
    INFO = "info",
    SUCCESS = "success",
    WARNING = "warning",
    ERROR = "error",
}

/** @summary Visual style modes supported by alert components. */
export enum AlertStyle {
    SOLID = "solid",
    OUTLINE = "outline",
    DASH = "dash",
    SOFT = "soft",
}

/** @summary Layout direction options for alert content and actions. */
export enum AlertDirection {
    HORIZONTAL = "horizontal",
    VERTICAL = "vertical",
    RESPONSIVE = "responsive",
}

/** @summary Width presets for modal components. */
export enum ModalSize {
    SM = "sm",
    MD = "md",
    LG = "lg",
    XL = "xl",
}

/** @summary Viewport anchoring options for modal dialogs. */
export enum ModalPosition {
    CENTER = "center",
    TOP = "top",
    BOTTOM = "bottom",
}

/** @summary Scroll ownership modes for modal body overflow behavior. */
export enum ModalScrollMode {
    INSIDE = "inside",
    BODY = "body",
}

/** @summary Semantic variants for toast notifications. */
export enum ToastVariant {
    INFO = "info",
    SUCCESS = "success",
    WARNING = "warning",
    ERROR = "error",
}

/** @summary Anchor positions for tooltip popovers. */
export enum TooltipPosition {
    TOP = "top",
    RIGHT = "right",
    BOTTOM = "bottom",
    LEFT = "left",
}

/** @summary Size presets for loader and spinner components. */
export enum LoaderSize {
    SM = "sm",
    MD = "md",
    LG = "lg",
}
