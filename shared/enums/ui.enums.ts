export namespace UI {
  export enum ButtonVariant {
    PRIMARY = "primary",
    SECONDARY = "secondary",
    GHOST = "ghost",
    DANGER = "danger",
  }

  export enum AlertVariant {
    INFO = "info",
    SUCCESS = "success",
    WARNING = "warning",
    ERROR = "error",
  }

  export enum LayoutMode {
    DEFAULT = "default",
  }
}
